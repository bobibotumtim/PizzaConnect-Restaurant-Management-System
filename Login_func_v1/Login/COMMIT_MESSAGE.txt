feat: Add logic to add items to existing orders

✨ Features:
- Add "Add" button in ManageOrders for orders with status 0, 1, 2 (Waiting, Ready, Dining)
- POS opens in EDIT mode when orderId parameter is present
- Load existing order items and mark them as "existing" to prevent re-insertion
- Filter only NEW items when submitting to backend
- Auto-update order status from Ready → Waiting when new items are added

📝 Changes:
1. ManageOrders.jsp
   - Add "Add" button for orders (status 0-2)
   - Link to pos?orderId={orderID}

2. pos.jsp
   - Add loadExistingOrder() function
   - Hide table selection panel in EDIT mode
   - Hide Clear button in EDIT mode
   - Mark existing items with isExisting flag
   - Update completeOrder() to send orderId for EDIT mode

3. POSServlet.java
   - Add handleGetOrderAPI() to return order details
   - Add addItemsToExistingOrder() method
   - Add parseCartItemsWithToppingsForEdit() to filter new items
   - Add saveToppingsForOrderDetail() for topping support

4. OrderDAO.java
   - Add addItemsToOrder() method
   - Insert new items into OrderDetail
   - Update Order's TotalPrice
   - Auto-update Order status (Ready → Waiting)

🎯 Benefits:
- Customers can order more items while waiting or dining
- Chef automatically sees new items (status changes to Waiting)
- Total price is updated correctly
- Existing items are preserved (not duplicated)

📚 Documentation:
- Added LOGIC_THEM_MON_VAO_ORDER.md with detailed flow and test cases
